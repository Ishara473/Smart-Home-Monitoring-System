# Backend & Cloud Services

This directory contains cloud backend scripts, rules, cloud functions, and background listeners for **Firebase**.

## Purpose

- Real-time database rules and schema definitions.
- Server-side safety listener / worker process enforcing `max_on_duration` limits on high-power devices (e.g. automatic `OFF` flip & push notification alert).
